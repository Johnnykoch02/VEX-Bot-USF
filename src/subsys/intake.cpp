#include "main.h"

// Note: This file has not been worked on, but here is some code that might help you set it up.

// //Helper Functions
// void setIntake(int left, int right) {
//   driveFrontLeft = left;
//   driveFrontRight = right;
// }
//
// //Driver Control Functions
//
// void setIntakeMotors() {
//   int leftJoystick = controller.get_analog(pros::E_CONTROLLER_ANALOG_LEFT_Y);
//   int rightJoystick = controller.get_analog(pros::E_CONTROLLER_ANALOG_RIGHT_Y);
//   if (abs(leftJoystick) < 10) leftJoystick = 0;
//   if (abs(rightJoystick)<10) rightJoystick = 0;
//   setIntake(leftJoystick, rightJoystick);
// }
